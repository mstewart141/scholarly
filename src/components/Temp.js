import React from 'react';
import { lol } from '../reducers/actionCreators';

const Temp = () =>
  <div>
    <div> hello </div>
    <button onClick={() => lol()}> hello </button>
  </div>;

export default Temp;
